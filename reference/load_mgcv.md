# Load mgcv quietly

Simple function that loads the *mgcv* package whilst suppressing the
startup messages that it prints to the console.

## Usage

``` r
load_mgcv()
```

## Value

Returns a logical vectors invisibly, indicating whether the package was
loaded or not.
