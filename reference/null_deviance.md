# Extract the null deviance of a fitted model

Extract the null deviance of a fitted model

## Usage

``` r
null_deviance(model, ...)

# Default S3 method
null_deviance(model, ...)
```

## Arguments

- model:

  a fitted model

- ...:

  arguments passed to other methods
